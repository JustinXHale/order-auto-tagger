name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "node scripts/with-local-database-url.mjs npx prisma generate"
dev = "node scripts/with-local-database-url.mjs npx prisma migrate deploy && npm exec react-router dev"
